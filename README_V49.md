# SecurePass V49 - Render Ready

Cette version retire les clés FedaPay du code et utilise les variables d'environnement.

Local:
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python3 app.py
```

Render: voir `RENDER_DEPLOYMENT_V49.md`.
